# TASK_022_frontend_graph_preview

## Цель

Добавить MVP UI предпросмотра графа для отношений знаний.

---

## Входной контекст

Прочитать перед кодированием:

- `docs/SDD.md`
- `docs/AI_RULES.md`
- `docs/HANDOFF.md`
- файлы предыдущих задач по применимости
- этот файл задачи

Соответствующий контекст:

- файлы предыдущих задач 001-021
- SDD React Flow и требования графовой навигации

---

## Файлы для создания или изменения

Ожидаемые файлы:

- `backend/app/api/routes/graph.py`
- `backend/app/api/router.py`
- `backend/app/graph/graph_query_service.py`
- `backend/app/schemas/graph.py`
- `frontend/src/api/graph.ts`
- `frontend/src/features/graph/GraphPage.tsx`
- `frontend/src/App.tsx`

Добавлять минимальные вспомогательные файлы только когда требуется импортами/тестами.

---

## Требования

- Предоставить backend endpoint запроса графа для небольшого окружения по ID сущности/документа.
- Применять фильтрацию доступа к графовым запросам.
- Отображать узлы/рёбра в frontend с использованием React Flow или простой запасной визуализации.
- Показывать метаданные источника для выбранного ребра/узла, где доступно.

---

## Явно не делать

Не делать:

- возвращать неограниченные данные графа
- выводить новые графовые факты
- вызывать LLM из UI графа
- строить полный редактор графа
- изменять `docs/SDD.md`
- коммитить реальные секреты или приватные данные

---

## Команды валидации

Из корня репозитория, адаптировать под текущий этап:

```bash
cd backend
python -m pytest
python -m compileall app
```

Если изменены frontend файлы:

```bash
cd frontend
npm install
npm run build
```

Если требуются Docker сервисы и они доступны:

```bash
docker compose up -d
```

---

## Definition of Done

- Graph endpoint возвращает схему узлов/рёбер.
- Frontend отображает предпросмотр графа.
- Фильтрация доступа присутствует.
- Метаданные источника видны.
- `docs/HANDOFF.md` обновлён Claude Code с Gemma-4-31B.

---

## Ожидаемое обновление handoff

Claude Code с Gemma-4-31B должен обновить `docs/HANDOFF.md`:

- статус задачи;
- изменённые файлы;
- команды валидации и результаты;
- заметки по выполнению;
- заглушки/заполнители;
- известные проблемы;
- следующая задача: `TASK_023_markdown_export.md`;
- готовность к коммиту.
